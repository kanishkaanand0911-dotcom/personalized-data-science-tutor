# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# Fuzzy string matching for category normalization.
# NOTE: this codebase currently uses Python's built-in difflib as a
# rapidfuzz substitute (no internet access during development). Swap to
# rapidfuzz in your real environment -- faster and more robust on messy
# real-world strings. Uncomment below once installed:
# rapidfuzz>=3.0

# Only needed once Person B wires in real LLM narration/tie-breaking calls:
# anthropic>=0.25  -- only if using the Anthropic version of narration
google-genai>=0.1  # for Gemini-based narration (recommended: genuinely free tier)
python-dotenv>=1.0  # loads .env automatically -- required for the API key to actually be picked up

# Web layer (web/ - FastAPI wrapper + gamified frontend, replaces the Streamlit plan):
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9  # required for file upload on /api/dataset
requests>=2.31  # calls the optional ADK tutor connector (web/chat.py::ask_adk)

# Testing
pytest>=7.4
httpx>=0.27  # fastapi TestClient
